import { createAsyncThunk, createSlice } from "@reduxjs/toolkit";
import axios from "axios";

// export const addWit

const exploreWits = createSlice({
  name: "explore",
  initialState: [],
  reducers: {},
  extraReducers: (builder) => {},
});
